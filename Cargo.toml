[package]
name = "bloom_filter"
version = "0.0.1"
authors = ["Will Johnston"]

description = "Space efficient probabilistic hash set"
repository = "https://github.com/wbjohnston/bloom_filter"

[dependencies]
bit-vec = "0.4.4"
